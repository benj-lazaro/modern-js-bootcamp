// Return Statement Demo

function add(x, y) {
    return x + y;       // Evaluates & returns a single value (i.e. the sum of two numbers)
}

const sum = add(10, 10);

console.log(`The sum of 10 and 10 is ${add(10, 10)}.`);
