// .getElementById() Demo

// Select an element object based on its ID attribute value
const img = document.getElementById("starship-photo");
// Display properties of retrieved element object
console.dir(img);
// Display the associated HTML element (tag & content)
console.log(img);

const p = document.getElementById("main");
console.dir(p);
console.log(p);
