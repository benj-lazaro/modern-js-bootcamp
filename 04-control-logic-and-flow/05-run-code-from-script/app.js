alert("It's working!");

// The result from this line of code will NOT show up anywhere
"hi".toUpperCase()

// The following lines of code prints result to the browser's console
// The .log method of the console basically logs/prints the results to the browser's console window
console.log("hi".toUpperCase());
console.log(3 + 4);

// You can pass multiple arguments to the .log() method of the console
console.log(10 + 10, "Hi there!", true);

// The .error method of the console returns the passed value like a JavaScript error
console.error("OH MY!!!");